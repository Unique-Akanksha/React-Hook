// Purpose: Track scroll position inside a scrollable container.

// Shows a scrollable box with lots of content.

// Logs last scroll position when a button is clicked.

// Focus: Using useRef to store mutable values without triggering re-renders.
